const game = require("./gamePlay");

// turn count is always > 0 and less than 200.
// same location firing is prohibited

it("Should end the game before all the squares are occupied", () => {
 
});

it("Should prevent the human from attacking the same square more than onece", ()=> {

});